# Tennis Homepage V2 독립 운영 전환 계획

- 작성일: 2026-09-17
- 상태: A단계 쓰기 경계 조사 및 상세 설계 초안 작성 완료. 공개·일정 관리·기본 정원 정책 사용자 확정 및 세부 구현 규칙 정리 완료. A단계 설계 완료, 다음은 B단계 구현. 재가동 후 동기화 무간섭 검증은 C단계에 남김. 독립 구조 구현·마이그레이션·데이터 이전은 미착수.
- 작업 경로: `/Users/dorm/coding/tennis-homepage-v2`
- 저장소: `cafeSowoo/tennis-homepage-v2`
- 현재 배포 주소: https://cafesowoo.github.io/tennis-homepage-v2/
- 이전 기록: [1차 계획 및 구현 이력](v2-phase-1-plan-2026-09-17.md), [회원 로그인·권한 초기 설계](v2-member-auth-implementation-plan.md)
- 상세 설계: [A단계 쓰기 경계·권한·RPC 계약](v2-independent-operation-design.md)

## 1. 문서 우선순위와 확정된 목표

앞으로의 개발 방향은 이 문서를 우선한다. 이전 문서는 완료 증거와 전환 전 구조를 설명하는 이력이며 삭제하지 않는다. 아래 권장 설계는 구현의 출발점이고, 미정 사항은 확정된 요구로 취급하지 않는다. 문서 작성 요청 자체는 DB 변경·데이터 이전·운영 전환·배포 요청이 아니다.

사용자가 명시한 목표:

- V2에서 회원들이 직접 일정을 만들고 수정·삭제하며 참석/불참과 댓글을 관리한다.
- 카카오 로그인은 유지하되, 카카오톡 일정 동기화에 의존하지 않는 홈페이지로 운영한다.
- 이미 만든 기능을 최대한 재사용하고 V1과 V2가 서로 간섭하지 않게 한다.
- V1과 기존 동기화는 V2 안정화까지 유지하며, 종료 여부와 시점은 이후 별도로 결정한다.

작업 경계:

- V1 폴더 `/Users/dorm/coding/tennis-homepage`, 기존 배포와 기존 DB 테이블·정책·동기화 작업을 임의로 변경하거나 삭제하지 않는다.
- 공유 Supabase의 다른 앱도 변경 범위에서 제외한다.
- 사용자 확인 없는 대규모 리팩터링, 관리자 자동 승격, 전체 과거 데이터 이전은 하지 않는다.
- `.textClipping`은 작업·스테이징·커밋에서 제외한다. 인증 토큰과 비밀 키를 문서·코드·로그에 노출하지 않는다.
- 배포 요청 시 기본은 GitHub commit/push와 Pages workflow 검증이다. Netlify는 명시적 요청 없이는 사용하지 않는다.

## 2. 출발점과 재사용 범위

2026-09-17 설계 검토에서 로컬 코드·계획서와 원격 DB 컬럼·정책·트리거를 확인했다. 아래는 당시 확인 결과이며 구현 전에 다시 확인한다.

- 현재 V1/V2는 `schedules`, `discussions` 등 운영 데이터를 공유한다. 기존 일정 출처는 Kakao 160개, 홈페이지 17개, seed 32개였다.
- Kakao 로그인, `club_member_accounts` 승인 및 `members` 연결, 본인 RSVP, 회원 댓글 작성·본인 삭제, V2 저장소 격리가 구현돼 있다.
- 기존 자동 테스트 12개와 DB rollback 테스트가 통과했다. 새 전용 테이블과 권한 구조는 아직 검증하지 않았다.
- 일정 생성·수정·삭제는 owner 이메일 기반이다. `host_id`는 있지만 작성자 전용 불변 식별자는 없다.
- 현재 일정 수정은 참석자 배열까지 함께 저장한다. 회원 편집을 열기 전에 일정 정보 수정과 RSVP 저장을 분리해야 한다.
- 현재 RSVP는 Kakao 원본·override·배열·불참 테이블을 조합한다. 독립 운영에서는 이 중복 저장 구조를 새로 만들지 않는다.
- 후속 A단계에서 실제 자동화 설정 `automation-4`가 ACTIVE이며 기존 `schedules`, `members`, `courts`, `court_units`를 대상으로 하고 댓글을 제외하는 것을 확인했다. 후속 조사에서 8월 26일 기존 일정 SQL 쓰기와 9월 14~17일 로그아웃으로 인한 변경 없음 보고를 확인했다. 현재 SQL 도구의 postgres/RLS 우회 권한도 확인했으나 과거 실행 역할과 동일함을 증명하지는 않는다.
- 기존 `court-images`, `schedule-photos`는 공개 bucket이다. 새 회원 전용 사진을 제공한다면 별도 private bucket이 필요하다. `/club/` 화면의 anon 조회와 공개 JSON fallback도 전환 범위에 포함한다.

재사용: 로그인과 세션 격리, 회원 승인 연결, 달력·상세·작성 화면, 댓글 UI, 회원 권한 확인 및 행 잠금의 검증 원칙.

변경: V2 일정/참석/댓글 데이터 접근, 작성자 권한, 일정 수정 저장 범위, 서버 입력 검증. 기존 RPC·정책을 덮어쓰지 않고 새 경로를 추가한다.

## 3. 권장 데이터 경계

**같은 Supabase 프로젝트를 사용하되, V2 일정·참석·댓글은 별도 테이블로 분리한다.** 테이블명은 아래를 설계안으로 사용하며 실제 SQL과 마이그레이션은 아직 작성하지 않는다.

| 영역 | V1 및 기존 동기화 | V2 독립 운영 |
| --- | --- | --- |
| 인증·회원 연결 | 기존 Google 관리자 유지 | 기존 Kakao Auth·승인 연결 재사용 |
| 일정 | 기존 `schedules` | 새 `v2_schedules` |
| 참석 | 기존 배열·override·불참 테이블 | 새 `v2_schedule_rsvps` |
| 댓글 | 기존 `discussions`·Kakao 원본 | 새 `v2_discussions` |
| 회원·코트 | 기존 데이터 및 관리 경로 | 우선 공통 참조, 일반회원 수정 금지 |
| 사진 | 기존 저장 경로·권한 유지 | 회원 업로드 제공 시 V2 전용 경로 또는 bucket |

기존 테이블에 `source=v2`만 추가하는 안은 V1의 조회·owner 권한·동기화 로직까지 구분값을 지켜야 하므로 채택하지 않는 것을 권한다. 별도 Supabase 프로젝트는 가장 강한 격리지만 인증·승인·사진 이전 비용이 커 현재 우선안에서 제외한다.

같은 프로젝트의 테이블 분리는 완전한 격리가 아니다. Auth 설정, 회원 활성 상태, 코트 정보, 프로젝트 장애와 높은 권한의 DB 작업은 여전히 공유된다. V2 도입을 이유로 공통 정책을 광범위하게 바꾸지 않는다. 높은 권한의 동기화 작업에는 RLS만으로 격리가 보장되지 않으므로 실제 쓰기 대상을 확인한다.

### 데이터 원칙

- 일정: UUID 등 충돌 없는 ID, 제목, 날짜·시작/종료 시각, 코트, 작성자 회원 ID, 진행자 회원 ID, 정원, 모집 상태, 취소 상태, 생성/수정 시각, 수정 버전. 작성자와 진행자는 구분한다.
- 작성자는 서버가 로그인·승인 연결로 결정하며 일반 편집으로 변경할 수 없다. 인증 계정 ID를 감사 정보로 남기는 세부 방식은 구현 설계에서 정한다.
- RSVP: `(schedule_id, member_id)`당 한 행, 참석/불참/미정 상태. 행이 없는 회원도 미응답으로 표시할 수 있게 의미를 정한다. 참석자 배열을 별도 원본으로 중복 관리하지 않는다.
- 정원은 기본 16명으로 하고 일정 편집 권한자가 일정별로 변경할 수 있다(사용자 확정). 기존 16명 고정 상한은 새 시스템에 적용하지 않는다.
- 댓글: V2 일정·작성 회원에 연결하고 본문 길이·공백·승인 상태를 서버에서 검증한다. Kakao 원본 댓글을 새 회원 작성 댓글로 취급하지 않는다.
- 일정 정보 변경은 RSVP를 갱신하지 않는다. 참석 변경·정원 축소·일정 취소가 같은 일정에 대해 일관된 잠금 규칙을 사용해야 한다.
- `version` 등으로 오래된 화면의 일정 수정 충돌을 감지하고 재확인을 안내한다. 중복 생성·전송과 실패 재시도도 검증한다.
- 일정이 취소되면 새 RSVP와 댓글을 허용할지 명시적으로 정한다. 구현 기본값은 새 쓰기 차단 및 기존 댓글 본인/관리자 삭제 허용이다. 상세 설계의 상태별 규칙을 따른다.
- 기존 `events`의 일반 행사 기능은 이번 핵심 범위에서 제외한다. V2 독립 운영에서 이를 노출할 경우 기존 공유 테이블에 쓰는 경로를 남기지 않도록 별도 결정한다.
- 원격 저장 실패를 로컬 저장 성공으로 표시하지 않는다. 시험용 로컬 모드와 실제 회원 운영 모드를 구분한다.

## 4. 권장 권한 모델

| 사용자 | 일정 | RSVP | 댓글 |
| --- | --- | --- | --- |
| 비로그인·pending·disabled·비활성 회원 | 새 V2 일정 열람·쓰기 불가 | 열람·변경 불가 | 열람·작성·변경 불가 |
| 승인된 활성 회원 | 생성, 본인 작성 일정 수정·취소 | 본인 상태만 변경 | 작성·본인 삭제, 수정 제공 여부 미정 |
| 승인된 관리자 | 전체 일정 관리 | 타인 응답 대리 변경은 초기 범위에서 제외 | 운영상 필요한 댓글 관리 |

모든 회원에게 타인의 일정 편집을 허용하지 않는 것을 기본안으로 한다. 관리자 판정은 승인 상태·활성 회원·역할을 사용하고 로그인 Provider나 닉네임을 권한 근거로 삼지 않는다.

김지석 Kakao 계정은 현재 `approved/member`로 유지한다. Google 관리자와 같은 회원에 연결돼 있어도 관리자 권한을 자동 승계하지 않는다. 운영 관리자 계정 선정·승격은 별도 명시적 결정 사항이다.

RLS·테이블 grants·RPC 검증을 함께 설계한다. 클라이언트가 작성자, 다른 회원 ID, 상태를 위조해도 서버에서 거부해야 한다. 승인/역할 수정은 관리자 경로만 허용한다. 기존 owner 정책을 새 테이블에 무조건 복제하지 않는다.

## 5. 확정 정책과 남은 미정 사항

사용자가 “세 가지 모두 추천대로”라고 답해 다음을 확정했다.

- 새 V2 일정 상세·명단·댓글은 승인된 활성 회원만 열람한다. V1의 기존 공개 범위는 유지한다.
- 일정은 승인된 활성 작성자와 관리자만 수정·취소하며 기록을 보존한다. 일반 운영 UI에 완전 삭제는 제공하지 않는다.
- 기본 정원은 16명이며 일정 편집 권한자가 일정별로 변경할 수 있다.

아래 항목은 추가 구체화가 필요하다. 세 가지 선택을 다른 정책이나 사용자 역할 변경에 대한 승인으로 확대하지 않는다.

| 항목 | 권장 기본안 | 확정이 필요한 시점 |
| --- | --- | --- |
| 모집·시간 기준 마감 | 마감 후 신규 참석 차단, 기존 참석 철회 허용. 시작 이후 RSVP 변경 차단 | 구현 기본값 정리 완료 |
| 취소 후 쓰기 | 새 쓰기 차단, 기존 댓글 본인/관리자 삭제 예외 | 구현 기본값 정리 완료 |
| 댓글 수정 | 기존 작성·본인 삭제부터 유지, 수정은 후속 여부 결정 | 댓글 기능 이식 전 |
| 승인 관리 | 운영 전 관리자용 최소 승인·비활성화 UI 마련 권장 | 회원 초대 전 |
| 관리자 계정 | 현재 역할 유지, 운영에 사용할 계정은 명시적으로 선정 | 관리자 기능 시험 전 |
| 과거·미래 일정 이전 | 과거는 보존, 필요한 미래 일정만 일회성 이전 | 전환 기준일 결정 전 |
| 기존 참석자 이전 | 이전 출처 표시, 필요하면 V2 재응답 요청 | 데이터 이전 전 |
| 공통 회원·코트 변경 | 기존 경로 유지, V2 일반회원은 참조만 | 관련 편집 기능 추가 전 |

## 6. 단계별 실행 계획과 완료 기준

### A. 세부 설계와 쓰기 경계 확정

- [x] 기존 코드·원격 DB·계획의 구조 검토 및 새 목표 정리.
- [x] 독립 운영 전환 계획을 기준 문서로 작성하고 이전 기록 연결.
- [x] 실제 동기화 설정, 기존 코드 쓰기 경로와 원격 Storage 의존성 확인. 상세 설계 문서에 증거와 한계를 기록.
- [x] 실행 기록에서 실제 쓰기 대상과 최근 수집 불가 상태 확인. 현재 SQL 도구 권한 조회 및 과거 역할 검증 한계 기록.
- [x] 새 V2 공개 범위·작성자/관리자 수정·취소 및 기록 보존·기본 16명/일정별 변경 정책 사용자 확정.
- [x] 모집·시간 기준 마감과 취소 후 댓글 처리 등 세부 운영 규칙을 구현 기본값으로 정리.
- [x] 새 스키마·RPC·RLS·사진 권한과 화면별 저장 경로의 초안 작성.
- [x] 세 가지 사용자 선택을 상세 설계의 열람·편집·정원 기준에 반영.
- [x] 세부 규칙과 실행 기록의 경계를 반영해 A단계 구현 계약 정리. 실제 재가동 후 검증은 C단계로 분리.

완료 기준: 각 화면이 읽고 쓰는 테이블, 작성자와 관리자 권한, V1에 영향을 주지 않는 변경 범위를 설명할 수 있다.

### B. V2 전용 저장 구조와 회원 일정 관리

- [ ] 추가 마이그레이션으로 전용 테이블·제약·권한·RPC 구현. 기존 구조 유지.
- [ ] 기존 UI를 새 조회/저장 경로에 연결하고 회원 일정 생성·본인 수정·취소 구현.
- [ ] RSVP·댓글을 새 일정에 연결. 기존 UI·승인 검증 원칙 재사용.
- [ ] 기존 일정은 필요 시 출처가 명확한 읽기 전용 영역으로 표시. 기존 일정에 대한 V2의 RSVP·댓글 등 쓰기 경로도 이 단계에서 차단.
- [ ] 관리자 경로를 역할 기반으로 연결하고 필요한 최소 승인 관리 제공 범위 확정.

완료 기준: 승인 회원이 V2에서 생성부터 참석·댓글·취소까지 처리하며, 해당 작업이 기존 일정·댓글·참석 데이터를 변경하지 않는다.

### C. 권한·동시성·병행 운영 검증

- [ ] KakaoTalk 재로그인 후 동기화 재개 시 실제 쓰기 대상과 새 V2 테이블 무변경 확인. ACTIVE 설정만으로 무간섭 판정 금지.

- [ ] 비로그인/pending/disabled/비활성 회원 쓰기 차단, 작성자 위조·타인 편집·역할 승격 차단.
- [ ] 마지막 한 자리 동시 참석, 중복 클릭, 참석과 정원 변경/취소 경쟁 검증.
- [ ] 오래된 화면의 일정 수정 충돌, 실패 재시도, 삭제/취소 후 기록 보존 검증.
- [ ] 사진 경로·삭제 권한 및 V1 저장 파일 보호 검증 (업로드를 제공하는 경우).
- [ ] V1 Google·V2 Kakao 같은 브라우저의 로그인/로그아웃, 기존 관리자 기능 회귀 확인.
- [ ] V1의 기존 서비스워커 캐시 동작으로 인한 잔여 간섭 확인. V1 무수정 범위에서 해소되지 않으면 정식 운영 전에 V2 별도 origin 등 대안 검토.

완료 기준: DB 권한 테스트와 실제 UI 검증을 모두 통과한다. 이전 12개 테스트 통과만으로 새 기능 검증을 대체하지 않는다. 실데이터 변경 없이 검증할 수 있는 항목은 테스트 데이터로 수행한다.

### D. 제한된 시험 운영

- [ ] 소수 회원과 새 일정으로 운영. 시험 일정은 V2에서만 관리.
- [ ] 기존 카카오 일정은 V1에서 유지하며 동일 일정의 양쪽 편집을 허용하지 않음.
- [ ] 운영자 승인·취소·문제 대응과 사용자 안내를 확인.

완료 기준: 실제 사용에서 일정 생성·변경·참석·댓글 흐름과 운영 대응을 확인하고 미해결 문제를 기록한다. 시험 기간과 참여자는 별도로 정한다.

### E. 전환 기준일 및 필요한 데이터 이전

- [ ] 기준일 이후 새 일정의 공식 생성·관리 장소를 V2로 지정.
- [ ] 기존 미래 일정별로 V1에서 마무리할지 V2로 이전할지 선정.
- [ ] 이전 전 백업·대상 목록·원본과 새 ID 매핑·건수 검증·중복 방지 절차 준비.
- [ ] 필요한 미래 일정만 일회성 이전. 재실행 시 중복 생성 금지. 원본 출처와 이전 시점 보존.
- [ ] 이전 참석 상태는 본인이 V2에서 직접 제출한 응답과 구분. 댓글의 작성자·출처·열람 범위도 보존.
- [ ] 동일 일정의 공식 관리 장소를 안내하고 V1에 남은 원본은 V2로 재동기화하지 않음. V1 화면/데이터 변경이 필요한 경우 별도 범위 확정.

완료 기준: 한 일정의 공식 관리 장소가 하나이며, 데이터 대조와 사용자 안내가 끝난다. 기존 홈페이지 댓글·응답도 이전 필요성을 검토해 누락하지 않는다.

### F. 안정화와 기존 운영 종료 판단

- [ ] 안정화 결과를 검토하고 V1·동기화 중지 여부를 별도로 결정.
- [ ] 종료 시 먼저 중지·보존하고, 과거 기록 보관 방식과 접근 범위 결정.
- [ ] 기존 테이블·정책 삭제는 이 계획에 포함하지 않음. 공통 Auth·회원·코트와 다른 앱 의존성은 계속 유지.

완료 기준: 운영 주체·기록 보관·복구 방법이 확인돼 있다. 종료 결정 전까지 기존 자동화를 임의로 끄지 않는다.

## 7. 복구 원칙

- 새 구조는 추가 방식으로 준비하고 전환 전 DB·필요 파일의 복구 가능한 백업을 확인한다.
- 시험 실패 시 V2 쓰기를 중지하고 V1의 기존 운영을 유지할 수 있어야 한다. 이때 생성된 V2 데이터는 삭제하지 않는다.
- 전환 후 장애가 나면 V2의 신규 일정·참석·댓글을 보존한 상태로 쓰기 제한과 복구를 우선한다. V1은 새 V2 데이터를 자동으로 갖고 있지 않으므로 V1로 단순 복귀하지 않는다.
- 코드 복구와 데이터 복구를 구분한다. 이전 커밋 배포가 V1 공유 테이블 쓰기를 다시 활성화할 수 있으므로 안전한 복구 배포 지점을 마련한다.
- DB 마이그레이션을 되돌리며 새 테이블을 삭제하는 방식은 기본 복구안으로 사용하지 않는다.

## 8. 현재 다음 작업

A단계 조사와 구현 계약 정리를 마쳤다. 다음은 **B단계: 새 V2 테이블·RPC·RLS 및 권한 테스트를 먼저 구현하고 기존 화면을 새 경로에 연결하는 작업**이다. 실제 동기화 재개 후 무간섭은 C단계에서 검증한다. 기존 계획의 `카카오 상태 따르기` 개발은 선행하지 않는다. 동시 RSVP 검증은 B의 새 저장 구조를 대상으로 C에서 수행한다.

이번 문서 작성에서는 코드, DB, Provider 설정, 사용자 역할, V1, 동기화 자동화, 배포를 변경하지 않았다.
